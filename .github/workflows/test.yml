name: Test
on:
  push:
    branches:
      - master
  pull_request:

jobs:
  test:
    name: helplint test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: true
      - name: helplint-check
        uses: ./
